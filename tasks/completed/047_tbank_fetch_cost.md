# Стоимость сбора tbank

> Создана 2026-09-05 по итогам сквозного ревью репозитория (13 линз, агенты). Объём S на главный пункт. Отложено автором в сессии 2026-09-01, теперь измерено.

## Что происходит сейчас

**Обычный запуск** (`scripts/backfill/fetch_tbank_dividends.py`): 316 тикеров с
существующим `.html` пропускаются мгновенно. Остальные **718 запрашиваются каждый
раз** — провалы не кэшируются, а `.fill_cache/tbank/_failures.json` (718 записей, все
`not_found`) скриптом не читается. Итог: ~718 запросов и 359 секунд чистого
`time.sleep` ради нуля новых данных.

**`--refresh`**: 1034 запроса, 316 отдают HTML средним размером 1.65 МБ (median 1.63,
max 1.97) — **520 МБ трафика за прогон**. Дельту запросить нельзя в принципе: страница
отдаёт всю историю одним bootstrap-блобом, параметра даты нет. Единственный рычаг —
**кого** спрашивать, не **что**.

## Профиль

| компонент | цифра |
|---|---|
| `SLEEP_BETWEEN = 0.5` × 1034 | **517 с** |
| скачивание 520 МБ | 80–210 с |
| чтение с диска + regex + `json.loads` 316 страниц | **6.1 с** |
| запись на диск | единицы секунд |

ЦПУ и парсинг ни при чём. Узкое место — self-imposed sleep и объём HTML.
`SLEEP_BETWEEN = 0.5  # seconds -> 2 req/s` (`fetch_tbank_dividends.py:35`) — **выдуманная
константа**: ни в коде, ни в скилле нет ни одного замера 429.

Полезная плотность: из 520 МБ HTML извлекается **359 КБ** JSON (1843 записи по 316
тикерам), коэффициент **1448×**. В `data/dividends/` доживает 464 строки.

## Ранжирование по «выигрыш / риск сломать данные»

**1. Не спрашивать тикеры, у которых страницы нет. Выигрыш 69% запросов. Риск ~0.**

Список уже существует и уже пишется — 718 записей `not_found`. Экономия **359 с с
каждого прогона**, включая «дешёвый» без `--refresh`, где это единственная трата.
Пропущенный 404 и несделанный запрос дают одинаковый результат — данные измениться не
могут.

Деградация одна: тикер появился в каталоге tbank после последнего полного обхода.
Лечится флагом `--full` (раз в квартал или при пополнении `tickers.json`) и полем
`checked_at` с TTL.

**Ключевая деталь:** файл лежит в `.fill_cache/`, которая не коммитится — после
`git clone` списка нет. Вынести в `data/dividends/_tbank_no_page.json`. Это заодно
попадает в цель «объяснения расхождений источников живут в репе»: «tbank не покрывает
эти 718 имён» — ровно такое объяснение, и сейчас оно живёт в невоспроизводимом кэше.

**2. Хранить извлечённый payload, а не HTML. Выигрыш 520 МБ → ~2 МБ. Риск low.**

Обязательная митигация: класть весь bucket `stores.investDividends.{T}`, а не готовые
записи — тогда переживают все брокерские поля (`declared`, период, тип выплаты).
Sanity-check в `fetch_tbank_dividends.py:93` работает по `r.text` в памяти, до записи,
так что логика «перезаписывать только при успешном payload» сохраняется. Экономии сети
не даёт — чисто дисковая мера.

**3. Ополовинить `SLEEP_BETWEEN` — только после замера. Риск medium (доступ, не данные).**

После п.1 остаётся 316 запросов = 158 с сна. Снижение до 0.25 срежет ещё ~80 с. Но
лимит не измерен: нужен осознанный прогон на 30–50 тикерах с логированием статусов.
Без замера это угадывание, а цена ошибки — блокировка IP на весь месячный refresh.

**4. Параллелизм — не делать до п.1.** После него остаётся ~5 мин, 4 воркера дадут ~2.
Выигрыш маленький, а цена — не «добавить asyncio», а переписать аккумуляцию: сейчас
там общий мутируемый `failures: dict` (`fetch_tbank_dividends.py:57`) и общие счётчики;
при наивном переносе на потоки `_failures.json` станет неполным.

**5. Сузить до ликвидной вселенной — НЕ делать.** `UNIVERSE_TOP_N_LIQUID = 100` —
скользящий срез, в 12-месячное окно входят имена, сегодня вне топ-100. Пропущенный
дивиденд у такого имени всплывёт как ошибка ранжирования через год, когда связь с
решением потеряна. Худшее отношение выигрыш/риск в списке.

**6. Дельта по датам — невозможна.** Страница не параметризуется.

## Целевое состояние

После п.1–2 ежемесячный `--refresh` — ~316 запросов, 3–5 мин, ~520 МБ трафика и **~2 МБ
на диске** вместо 520.

## Заодно

`src/ingest/dividends/yahoo.py:65` — `meta.get("currency", "RUB")`: дефолт не
срабатывает на `null`, а в кэше **32 из 266** payload'ов имеют `meta.currency = null`
(AVAZ, BISV, KRSG, MOTZ, OFCB, GRAZ). Повторный прогон каскада вернёт в данные дефект,
исправленный коммитом `9b0f34e`. Нормализовать на входе:
`(meta.get("currency") or "RUB").upper()` + `SUR -> RUB`, как уже сделано в
`tbank.py:106` и `iss.py:90`.

`_adjust_external` (`cascade_merge_dividends.py:136-163`) — сплит-коррекция внешних
источников — локальная замыкающая внутри `main()`. Её нет ни в `YahooFetcher`, ни в
`TbankFetcher`, ни в `fill.py`. Как только кто-то добавит их в `fetcher_map` в CLI,
коррекция тихо исчезнет, и по тикерам со сплитами (GMKN ×100, VTBR ×5000) записи уедут
на порядки. Та же судьба у чтения `_external_blacklist.json`.

---

## Итог (закрыто 2026-09-08)

Сделан п.1, но не так, как предлагала задача, и не он оказался главным.

### Блокер, найденный по дороге

`scripts/backfill/cascade_merge_dividends.py` падал на первом же тикере со сплитом:

```
AttributeError: '_Filtered' object has no attribute 'restates_splits'
```

Обёртка `_Filtered` проксирует `source_tag` и `fetch`, но не `restates_splits`, а
`fill.py:139` читает атрибут с того объекта, который ему передали, и **вне** `try`,
которым обёрнут `fetch`. Сломалось коммитом `a153a17`: он убрал `_adjust_external` и
завёл флаг `restates_splits`, а обёртку не тронул. `selected` идёт по алфавиту, ABRD
имеет `data/splits/ABRD.csv` — до отчёта скрипт не доживал.

Это шаг 3.2 месячного ранбука, то есть весь фолд-ин tbank не работал с 7 сентября.
Пропустили потому, что вызов стоял под `# type: ignore[arg-type]`, а `mypy` и так
покрывает только `src/`. Убрано и то и другое: `_Filtered` типизирован
`DividendFetcher`, `fetcher_map` объявлен как `dict[str, DividendFetcher]`.

### Что сделано по самой задаче

`fetch_tbank_dividends.py` не спрашивает делистнутые имена; `--full` возвращает старое
поведение.

| | было | стало |
|---|---|---|
| `--refresh` | 1034 запроса, 517 с сна | 276 запросов, 138 с |
| без флагов | 718 запросов, 359 с | 8 запросов, 4 с |

Признак берётся из `delisted_after` в `data/tickers.json`, а не из нового файла со
списком 404. `delisted_after` пересчитывается на каждом `momentum tickers refresh`
(`dictionary.py:284-287` его и ставит, и **удаляет**), то есть возвращение бумаги в
листинг чинится само. Список 404 пришлось бы сопровождать вручную: TTL, `checked_at`,
квартальный ритуал `--full` — три механизма вместо поля, которое уже есть.

Порог `MIN_LIVE_TICKERS`: пропуск и попадание в кэш неразличимы в итоговой строке, так
что покалеченный `tickers.json` выглядел бы как быстрый чистый прогон.

### Поправки к задаче

- **«Ежемесячный `--refresh`» — верно, а `scripts/README.md` врал.** README помечал
  весь `backfill/` как `done`, а `reconcile_dividends.md` шаг 3 требует запускать оба
  скрипта каждый месяц. Три документа описывали месячный прогон и расходились; строка
  в README и комментарий в `.gitignore` («tbank … wired to never re-fetch») исправлены.
  Именно это расхождение и позволило блокеру прожить месяц незамеченным.
- **«Ключевая деталь» про `git clone` не работает.** После клона `.fill_cache/tbank/`
  пуст, полный обход всё равно обязателен — скип-листу там нечего экономить. Ценность
  коммита списка была бы чисто документальной.
- **Задача сравнивала не те множества.** Из 718 `not_found` **710 делистнуты**, то есть
  предлагаемый файл на 99% дублировал бы `delisted_after`. Оставшиеся 8 (DGBZ, GAZC,
  GAZS, GAZT, KMTZ, KZMS, NPOF, SUMZ) формально в листинге, но последняя цена у них
  2010–2023 — 404 от tbank точнее собственного поля репы. Стоят 4 секунды, оставлены.
- **48 делистнутых имён страницу имеют** и перестают рефрешиться. Кэш не трогается,
  каскад читает их как раньше. Выплаты после делистинга нашлись только у INGR и VTRS —
  обе в USD, обе уже в `_external_blacklist.json` как чужой эмитент, и котировок на
  MOEX после делистинга нет. В total return попасть не могут.
- **Секция «Заодно» протухла наполовину.** `_adjust_external` больше не существует —
  сплит-коррекция уехала в `fill.py:139` за `restates_splits`, ровно туда, куда задача
  просила. Вторая половина абзаца жива: `_external_blacklist.json` применяется
  скрипт-локальной обёрткой, а не `fill.py`, так что при выносе фетчеров в CLI он
  тихо перестанет действовать.
- **Yahoo `meta.get("currency", "RUB")` (`yahoo.py:66`) — дефект настоящий, но мёртвый.**
  32 из 266 payload'ов имеют `currency = null`, но записи есть только у трёх (BISV,
  KRSG, MOTZ), все делистнуты; `YahooFetcher` в CLI не подключён, ранбук велит yahoo не
  трогать, а `merge.py:36` и `apply.py` нормализуют пустое в RUB. В `data/dividends/`
  пустой валюты нет ни строки (3054 RUB + 30 USD). Не трогал.
- **Пп. 2–6 не делались.** Хранение payload вместо HTML экономит только диск в
  гитигнорном кэше и требует перекачки 316 страниц ради конвертации. `SLEEP_BETWEEN`
  после п.1 стоит 138 с — замер лимита не окупает риск бана на месячном прогоне.
  Параллелизм после п.1 экономит минуты две ценой переписывания аккумуляции.

### Что осталось открытым

- Два «одноразовых» скрипта в `backfill/` — рекуррентная инфраструктура, что нарушает
  locked-правило двух каденций. Либо выносить в CLI, либо признать исключением явно;
  сейчас в README записано второе.
- `momentum ingest dividends --force-refresh` — 1034 запроса в месяц к эндпоинту,
  мёртвому с 2025-10 (`task 054`). Та же болезнь, что чинила эта задача, но на
  порядок дороже.
