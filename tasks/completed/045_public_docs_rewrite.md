# Публичные тексты: README, methodology, ограничения

> Создана 2026-09-05 по итогам сквозного ревью репозитория (13 линз, агенты). Объём M. Числа не меняются.

## Главная претензия по существу

`docs/methodology.md:89` утверждает про MCFTRR: «Совпадает с базой нашего расчёта,
прямое сравнение корректное».

Совпадает только **налоговая база**. Q-портфель равновзвешенный, ~25 имён; MCFTRR
капвзвешенный, в нём доминируют несколько голубых фишек. Часть того, что публикуется
как альфа Q1, — это эффект равного веса и малой капитализации, а не моментум. Документ
не умалчивает об этом, а прямо отрицает.

Второе неназванное ограничение: в издержках только комиссия 0.05% и налог 13%. Спреда
и проскальзывания нет — слово «спред» встречается только как «спред Q1−Q4». Ежемесячный
ребаланс равновзвешенного портфеля по хвосту top-100 российского рынка 2013 года
(медиана оборота сотой бумаги ~9 млн ₽/мес, цифра из того же документа) стоит по спреду
заметно больше комиссии.

## Структурное

1. **README на 36% состоит из runbook'а** (`README.md:52-131`), который вдобавок
   продублирован в `.claude/skills/update_monthly_data/references/*.md`, причём **там
   он подробнее**. При этом `SKILL.md:78-80` объявляет README каноном. Вырезать в
   `docs/RUNBOOK.md`, оставить три строки со ссылкой.
2. **Нигде нет результата.** Ни окна бэктеста, ни CAGR, ни спреда Q1−Q4, ни просадки.
   Человек с GitHub должен уйти на сайт и разглядывать график. Для проекта, чья
   ценность — эмпирический результат, это главная дыра. Добавить «Results at a glance»
   сразу после вводного абзаца.
3. **Ограничения размазаны** по буллетам раздела «Корпоративные действия» —
   `methodology.md:66,107,108,110,116`. Собрать в отдельный раздел «## Ограничения»
   перед «Источники данных». В README добавить «Limitations» из 5-6 однострочников
   **до** Quickstart. Сейчас там безусловное «**Survivorship-free.** No retrospective
   winners list» без единой оговорки.
4. **Нет дисклеймера.** При этом `methodology.md:75` прямо называет последнюю строку
   «actionable-инструкцией: портфель, который держится в течение месяца M». Публичная
   финансовая методика, предлагающая торговый состав, без оговорки о применимости.
   MIT покрывает софт, не содержание. Две строки в README перед License и в футер
   `base.html`.
5. **«Индекс магов» — целая вкладка сайта**, о которой ни README, ни methodology не
   знают. Читатель открывает сайт и видит незнакомый пункт меню.
6. **Quickstart даёт неполный рецепт**: три команды против шести в `pages.yml`.
   Результат — сайт без работающей страницы Experiments. И этот же рецепт напечатан
   ниже второй раз, в другой версии.
7. **README не показывает, как проверить код.** `pytest`, `ruff check`, `mypy src` не
   встречаются ни разу, хотя это ровно то, что гоняет `lint.yml`.
8. **Языковой разрыв не объявлен**: README английский и подаёт `docs/methodology.md`
   как «Methodology (full)», читатель кликает и попадает в русский текст.

## Дополнено 2026-09-07 (из закрытой `task 051`)

Раздел «## Ограничения» обязан содержать оговорку о переборе вариантов. Ни на одной
из восьми собираемых страниц не встречается ни «значим», ни «bootstrap», ни «p-value»,
при том что на главной нарисована линия `Q1 top15` — и `index.html` даёт её **без
единого слова текста**, только подписью в легенде. Читатель видит шестую линию, которая
обгоняет Q1, и не знает ни что это, ни что это одна точка сетки, показанной на соседней
странице, ни что превосходство не пережило поправку на множественные сравнения.

Формулировать **без цифр**: в репозитории записаны два противоречащих значения одного
и того же Reality-Check p (`024:151-152` — 0.086, `REPORT_024_topn_fan.md:100-101` —
0.043), и сегодня ни одно из них не воспроизводится. Публиковать любое из них нельзя.

Претензия про «лучшее из перебранного» при этом раздута: `config.py:34-37` и
`026:20-23` оба фиксируют, что K=15 выбран как продуктовое решение и **не** является
оптимумом в выборке, а `compare.html:31-34` аргументирует устойчивость по сетке, а не
победителя. Дыра — ровно в отсутствии текста на главной и раздела «Ограничения».

## Устаревшие факты

- `README.md:201` — «computed outputs (`data/computed/`)»; каталог называется
  `data/momentum/`.
- `README.md:169-170` — «constants for an experimental persistence strategy»;
  `task 023` удалила persistence целиком, констант нет.
- `README.md:145` и `methodology.md:120` — детектор назван «fail-loud»; реально
  `--strict` по умолчанию `False` и в CI не вызывается вовсе. `README.md:75` сам себя
  опровергает («Detector (WARN-only)»).
- `methodology.md:116` — «backtest не публикуется до 2010-01»; фактически
  `ANALYSIS_START_DATE = 2013-01-01`.
- `methodology.md:108` — список валютных дивидендов «INGR, POLY, RUAL, T», VTRS пропущен.
- `methodology.md:25` — «σ по log-доходностям»; код считает `window.std(ddof=1)` по
  простым total return. `methodology.md:27` — `r(12-1)` описан как `[t-12 … t-1]` при
  фактическом `[t-11 … t-1]`; строкой выше документ сам пишет правильное «11 точек».
- `methodology.md:50,54` отправляет на страницу «Q history»; в навигации она называется
  «Quartile composition».
- `CLAUDE.md:14,21` — `src/momentum/` как корень продакшн-кода и `src/momentum/config.py`;
  реально плоский `src/`, `src/config.py`. Это файл, который агент читает первым.
- `scripts/build_plotly_bundle/{build.sh:42,README.md}` — «paste into
  `src/momentum/viz/render.py:_BUNDLE_SHA256`»; реально `src/viz/site_builder.py:53`,
  `PLOTLY_BUNDLE_SHA256`.
- `src/ingest/prices.py:5-6` — «First non-empty = winner»; реально union досок с
  priority-дедупом (`prices.py:304-322`).
- 22 упоминания JSONL в докстрингах при CSV-хранилище, включая **имена функций**
  `apply_conflicts_to_jsonl` и `cleanup_jsonl_near_duplicates`.
- `src/adjustments/dividend_gaps.py:7` объясняет своё расположение через пакет
  `corporate/`, которого не существует.
- `tasks/completed/000` раздел «Design decisions (locked)» противоречит коду в четырёх
  пунктах (top-N по обороту, union досок, JSONL, «всё под data/ коммитится»), а
  `CLAUDE.md` предписывает читать его перед любой работой как источник правды.

## Объёмы

| документ | сейчас | предложено |
|---|---:|---:|
| `README.md` | 220 | ~130 |
| `docs/methodology.md` | 121 | ~115 |
| `mfd_backfill/README.md` | 98 | ~25 |
| `scripts/build_plotly_bundle/README.md` | 110 | ~55 |
| `scripts/README.md` | 14 | ~25 |
| новый `docs/RUNBOOK.md` | — | ~85 |


---

## Ход работы

### Сделано 2026-09-07 — свод устаревших фактов

Из четырнадцати заявленных фактов двенадцать подтвердились, один (список валютных
дивидендов) был уже починен коммитом `a153a17`, один ложный: функции
`cleanup_jsonl_near_duplicates` не существует — её удалила `task 034`.

- `CLAUDE.md` — раскладка исправлена на плоский `src/`, константы на `src/config.py`,
  в описание `docs/` добавлены `mages_intro.md` и `mages_methodology.md` (они входы
  сборки, а не артефакты — этого пункта в задаче не было).
- `tasks/completed/000` — четыре залоченных решения **не переписаны**, а помечены
  строкой «Отменено (`task NNN`)»: top-N по ликвидности, union досок, CSV вместо
  JSONL, `data/momentum/` в gitignore. Переписывание журнала решений уничтожает
  историю, ради которой он и ведётся. `CLAUDE.md` предупреждён, что пометка сильнее
  решения над ней.
- Формулы в `methodology.md` **и** в `README.md`: окна `[t-11..t-1]` и `[t-5..t-1]`
  вместо `t-12`/`t-6`, σ по простым доходностям вместо логарифмических. Сверх задачи:
  `r(12-1)` — среднегеометрическая месячная доходность, а не total return за период,
  как утверждали оба документа.
- «Q history» → «Quartile composition»; окно бэктеста 2010-01 → 2013-01.
- README: Quickstart дополнен до шести команд (по трём собирался сайт без страницы
  Experiments), в таблицу CLI добавлены `compute sweep` и `compute fan`, снят
  «fail-loud» в трёх местах, `data/computed/` → `data/momentum/`, убраны константы
  несуществующей persistence-стратегии, объявлен язык методологии.
- Заголовки модулей: `prices.py` («first non-empty = winner» → union с приоритетным
  дедупом, `.jsonl` → `.csv`), `dividend_gaps.py` (пакет `corporate/` не существует).
- `scripts/build_plotly_bundle/{build.sh,README.md}` — путь к константе SHA.
- Пятнадцать неверных упоминаний JSONL в `src/` исправлены; пять верных
  (`data/tickers_unavailable.jsonl`) оставлены. Функция `apply_conflicts_to_jsonl`
  переименована в `apply_conflicts_to_file` — имя врало.

### Сделано 2026-09-07, вторая половина (после закрытия `task 050`)

Раздел «## Ограничения» в `methodology.md` пишет `050` (налог на прирост, ёмкость),
а `045` его дополняет — не переписывает. Туда же ложатся: равновзвешенность против
капвзвешенного MCFTRR, отсутствие спреда и проскальзывания, делистинг по 0%,
оговорка о переборе вариантов из закрытой `051`.

Отдельно, не зависит от `050`:

- Переписать секцию «Бенчмарк» (`methodology.md:88-90`). Сейчас: «Совпадает с базой
  нашего расчёта, прямое сравнение корректное». Совпадает только налоговая база.
  Тот же текст-источник лежит в `tasks/completed/000:17` — пометить и там.
- Дисклеймер: `methodology.md:75` прямо называет последнюю строку «actionable-инструкцией».
  Две строки в README перед License плюс футер в `base.html` — **футера там сейчас нет,
  его надо создать**.
- Две фразы на `index.html` про линию `Q1 top15`: что это и что это одна точка сетки.
  Оба факта уже записаны в `config.py:34-37` и `026:20-23`, решать нечего.
- Рунбук: последовательность команд остаётся в README (запуск руками нужен), триаж
  уходит в справочники скилла, `SKILL.md` разворачивается — сейчас он объявляет каноном
  менее подробную копию. Нового файла `docs/RUNBOOK.md` не заводить.
- «Results at a glance» — только генерацией в `index.html` при сборке. Руками нельзя:
  `050` записала «Q1 terminal = 11.58x», в эталоне сейчас 12.25x — протухло за двое суток.


Вторая половина закрыта тем же днём, после `050`:

- **«Бенчмарк» переписан.** Было «Совпадает с базой нашего расчёта, прямое сравнение
  корректное». Стало: налоговая база общая, схема весов — нет; разрыв Q1−MCFTRR это
  моментум **плюс** эффект равного веса и меньшей капитализации, разделить их можно
  только cap-weighted версией Q1.
- **Раздел «Ограничения» дополнен** четырьмя пунктами поверх написанного `050`:
  равновзвешенность против капвзвешенного индекса, делистинг по 0%, отбрасывание
  нерублёвых выплат, незначимость перебора вариантов (из закрытой `051`). Дубли с
  разделом «Корпоративные действия» свёрнуты в ссылки — там же лежали более точные
  формулировки, их и оставил.
- **Дисклеймер** в футере `base.html` (футера не существовало, создан) и разделом в
  README перед License.
- **Линия `Q1 top15` на главной объяснена**: что это, что это одна точка сетки, и что
  выбрана она как продуктовое решение. Ссылка на Experiments условная — страница
  опциональна, безусловная ссылка воспроизвела бы баг, закрытый в `043`.
- **«Results at a glance»** — генерацией при сборке, не руками: за N лет Q1 дал ×X
  (Y% годовых) против ×Z у MCFTRR. Числа берутся из `q_values`, сверены с эталоном.
  Руками нельзя: `050` записала терминал, который протух за двое суток.
- **README**: блок с командами проверки как в CI, упоминание вкладки магов.
- **Рунбук разделён по назначению.** Последовательность команд осталась в README —
  запуск руками нужен. Триаж дивидендов (сорок строк про то, какому источнику верить,
  когда безопасен `augment`, какие кэши нельзя удалять) убран: он дублировал
  `references/reconcile_dividends.md`, который вдвое подробнее и обновляется. `SKILL.md`
  развёрнут — он объявлял каноном менее подробную копию. Нового файла `docs/RUNBOOK.md`
  не заводил.

Сокращение README до 130 строк как самоцель не делал: он 236 строк, и это честный
размер после того, как из него убрали дублирующийся триаж и добавили дисклеймер,
проверки и вкладку магов. Длина README никого не вводит в заблуждение.
