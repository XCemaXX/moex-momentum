# Precompute в CLI; `viz/` перестаёт считать стратегию

> Создана 2026-09-05 по итогам сквозного ревью репозитория (13 линз, агенты). Дорожка A. Объём M. После `task 042`.

## Проблема 1: правило двух каденций нарушено, и документ это отрицает

`scripts/README.md:3` утверждает буквально: «nothing here is imported by `src/`,
exercised by tests, or **run in CI**». А `.github/workflows/pages.yml:40,43` запускает
`scripts/compute_weight_sweep.py` и `scripts/compute_topn_fan.py` **на каждый push в
main**, и без них `compare.html` не рендерится (`src/cli/site_cmd.py:61-69`).

Это не one-shot, а часть регулярной сборки, живущая вне CLI: захардкоженные пути
(`compute_weight_sweep.py:29-32`), `print()` вместо логгера, `assert` как
продакшн-гейт (`compute_topn_fan.py:82` — исчезнет под `python -O`).

## Проблема 2: `viz/` считает стратегию в момент рендера HTML

`src/viz/site_builder.py:205-254` (`_q1top15_nav`) грузит панель месячных доходностей
(`load_panel`), ранжирует по скору (`score_ranking`) и прогоняет полный NAV-цикл
(`nav_from_selections`). Слой визуализации импортирует `momentum.topn_fan`,
`momentum.universe`, `momentum.transitions` и `config.COMMISSION_PER_SIDE`.

Кривая top-15 — самая быстрорастущая линия на главной (NAV 8.30 против 6.00 у Q1) —
нигде не сохраняется и не покрыта регрессией на уровне артефакта. То же делает
`_mages_page_context:323`.

## Что сделать

1. `momentum compute sweep` и `momentum compute fan` как subcommands; пути через
   `typer.Option`; `assert` → явный `raise`/`typer.Exit`. CI и README зовут CLI.
2. Кривую top-15 считать в `momentum compute backtest` и писать рядом с `q_values.csv`;
   `viz` получает готовый ряд через `series_registry`, как sweep и fan.
3. То же для mages: `momentum compute mages` пишет `data/mages/curve.csv`, страница
   только читает.
4. Переписать `scripts/README.md` в таблицу «скрипт | категория | когда запускать» по
   всем восьми позициям — сейчас перечислены три.

## Заодно

- `src/cli/site_cmd.py:81-83` хардкодит `mages_dir`, `indices_dir`, `monthly_dir`, при
  том что остальные шесть путей выведены в `typer.Option`.
- `build_site` — 19 keyword-параметров, из них 12 `Path | None` в паттерне
  «есть файл → путь, нет → None»; вызывающая сторона — 20 строк `x if x.exists() else None`.
  Свести к `@dataclass(frozen=True) SiteInputs` с классметодом `from_dirs(...)`.
- `NAV_LINKS` (`site_builder.py:56`) статичен, а `compare.html` и `mages_index.html`
  рендерятся условно: у клонировавшего в шапке каждой страницы висят ссылки на 404.
  Тест `tests/test_site_builder.py:133` этот дефект **закрепляет**, требуя ссылку на
  каждой странице.

## Инвариант

Сайт визуально не меняется. `q_values.csv`, `q1_nav.csv`, `fan_concentration.csv` —
байт-в-байт.

---

## Итог (2026-09-07)

Постановка проверена агентом-скептиком до начала работы. Что оказалось неверным:

- **Про `print()` вместо логгера — устарело.** Оба скрипта звали `setup_logging()`;
  это починила `task 027`. Оставались только строки прогресса.
- **`assert` под `python -O` — теория.** Ни один workflow не использует `-O`. Хуже:
  `BASELINE_TOPN` равен `UNIVERSE_TOP_N_LIQUID`, а CI пересчитывает `q_values.csv`
  прямо перед запуском, так что проверка сравнивала `backtest()` сам с собой и
  сработать не могла. Она полезна локально — против устаревшего файла на диске.
- **`compare.html` не «не рендерится», а сознательно пропускается** с сообщением
  (`site_cmd.py`), сборка при этом успешна.
- **Инвариант «байт-в-байт» снова перечисляет файлы, которых нет в git.**
- В `scripts/` девять позиций, не восемь.

### Сделано

- `momentum compute sweep` и `momentum compute fan`; логика — в `momentum/research.py`,
  команды тонкие. Пути через `typer.Option`, `assert` заменён на `raise` с внятным
  текстом и выходом 2. Скрипты удалены, `pages.yml`, `README.md` и подсказка в
  `site_cmd.py` зовут CLI. Оба CSV пересчитаны и совпали с прежними байт-в-байт.
- `tests/test_research.py` — раньше эти два шага сборки не были покрыты ничем.
- `scripts/README.md` переписан таблицей; фраза «nothing here is run in CI» теперь
  верна по построению, а не потому что её поправили.
- `NAV_LINKS` больше не статичен: ссылки на условные страницы выпадают вместе со
  страницами. Тест, который требовал ссылку на каждой странице и тем самым закреплял
  битые ссылки, заменён на два — отрицательный и положительный.
- `_q1top15_nav` уехал из `viz/site_builder.py` в `momentum/topn_fan.py` как
  `top_k_nav_from_scores`. Слой визуализации больше не крутит NAV-цикл и потерял
  четыре импорта: `nav_from_selections`, `score_ranking`, `load_panel`,
  `COMMISSION_PER_SIDE`.

### Отклонено

- **Пункты 2, 3b, 3c — кривые top-15 и магов в CSV.** Отменяют решение `task 026`,
  записанное под заголовком «Источник данных (**решено**)»: «без новых CSV — ни
  коммитимых, ни регенерируемых», причём `fan_concentration.csv` там отвергнут
  поимённо. Главный довод — «появится эталон на уровне артефакта» — не работает:
  `data/momentum/` в `.gitignore`, такой CSV пересоздаётся при каждом клоне и не
  является эталоном. Проблема слоёв решена переносом функции, без новых артефактов.
- **`SiteInputs`.** У `build_site` один продакшн-вызов и один тестовый; рефакторинг
  двенадцать условий не удаляет, а переносит, на пути рендера публикуемого сайта.
- **Эталонные тесты для кривых top-15 и магов.** Решение владельца: непокрытыми
  остаются только механики `nav_from_selections` и `mages_nav`, обе заморожены
  решением по `task 042`, а всё, что в них втекает, уже под голден-тестом.
