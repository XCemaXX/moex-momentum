# Зависимости, версии, настройки инструментов

> Создана 2026-09-05 по итогам сквозного ревью репозитория (13 линз, агенты). Объём M. Числа не меняются (кроме отдельно помеченных пунктов).

## Констрейнты не защищают от того, от чего должны

| пакет | объявлено | в локе | проблема |
|---|---|---|---|
| pandas | `>=2.2` | **3.0.2** | диапазон спанит границу 2→3, где сменились дефолты Copy-on-Write и строковый dtype |
| plotly | `>=5.22` | **6.7.0** | диапазон покрывает plotly.js от 2.x до 4.x, а закоммичен бандл 3.5.0 |
| numpy | **не объявлена** | 2.4.4 | импортируется напрямую в `signals.py`, `significance.py`, тестах |
| beautifulsoup4 | `>=4.12` | 4.14.3 | **ноль употреблений** в репо; тянет 5 пакетов |

Про plotly отдельно: SHA-тест бандла проверяет только, что файл не подменили. Он **не
поймает рассинхрон** — при `uv lock --upgrade` поедет Python-сторона, а файл останется.
Сузить до `>=6.7,<7` и добавить ассерт на `plotly.__version__` рядом с SHA.

**plotly 7 брать нельзя:** сменён движок цвета TinyColor → culori, цвета текста и
границ выбираются по контрасту WCAG, что даёт переключения тёмное↔белое на насыщенных
заливках — бьёт по подписям Sankey. Плюс plotly.js 4 требует Node ≥22, локально 18.19.1
(EOL). Промежуточный безопасный шаг при желании свежести — plotly 6.9.0 / plotly.js 3.7.0.

## CI использует не тот флаг

`uv sync --frozen` означает «синхронизируй по локу, не обновляя его» — он **не
проверяет**, что лок соответствует `pyproject.toml`. Правишь зависимость, забываешь
перелочить — CI зелёный, окружение старое. Нужен `--locked`.

Тот же дефект в `scripts/setup.sh:30`: `uv sync --frozen 2>/dev/null || uv sync` —
ошибка подавлена, фолбэк тихо резолвит заново и **перезаписывает `uv.lock`** в свежем
клоне.

## Лок протух на ~4 месяца

Единственный коммит по `uv.lock` — `fb6c535`. mypy 1.20.2 → 2.3.1 (мажор), ruff
0.15.12 → 0.16.6, typer 0.25.1 → 0.27.2, pytest 9.0.3 → 9.1.1, lxml 6.1.0 → 6.1.3
(два security-релиза: CVE-2025-7424, CVE-2025-11731 в libxslt; lxml парсит недоверенный
HTML с dohod.ru), pandas 3.0.2 → 3.0.5, numpy 2.4.4 → 2.5.2.

Двумя коммитами: (а) всё кроме plotly и mypy; (б) отдельно mypy 2.x, где включились
по умолчанию `--local-partial-types` и `--strict-bytes`.

## Что ещё

- **Даровое:** `filterwarnings = ["error"]` — замерено, 619 passed / 0 warnings,
  включается бесплатно. Плюс `--strict-markers --strict-config`.
- `.python-version` отсутствует; `requires-python = ">=3.12"` без верхней границы —
  на машине с 3.13/3.14 `uv sync` выберет другую ветку резолюции из того же лока.
- `pip install uv` в обоих workflow без пина. Заменить на `astral-sh/setup-uv` с пином
  и кешем.
- mypy не покрывает `scripts/` (`files = ["src"]`), хотя два скрипта оттуда считают
  числа для сайта в CI. Там же два **мёртвых** `# type: ignore[arg-type]`
  (`compute_topn_fan.py:65,88`) и три настоящие ошибки в `compare_formulas.py:68,132,181`.
- `pages.yml` ставит dev-зависимости — нужен `--no-dev`.
- `pytest-cov` объявлена и нигде не включена.
- ruff: дешёвый добор `C4, SIM, PERF, DTZ, PTH, FURB, A, W, NPY, TID, ISC, RET` —
  суммарно ~34 механические правки. `DTZ` и `PD011` смотреть глазами: касаются кода,
  считающего даты и серии.
- npm: `package-lock.json` в gitignore — сборка бандла невоспроизводима, транзитивное
  дерево не зафиксировано. Коммитить лок, `npm ci`. esbuild 0.24.0 (2024-09) → 0.28.2.
  jsdom в `dependencies` вместо `devDependencies`, отстал на 8 мажоров.
- actions/checkout и setup-python: v6 → v7.

## Бандл трогать не надо

Пересобран независимо — sha256 совпал байт-в-байт, сборка воспроизводима. Замеренные
варианты оптимизации: `--legal-comments=none` −365 B, `--drop:console` −269 B,
`--target=es2020` **+39 B**, всё вместе −634 B raw / −284 B gzip. Это **0.05%**.
1.23 МБ — близко к полу для scatter + bar + sankey. Отделять sankey во второй бандл
плохо: сейчас один файл кешируется между всеми восемью страницами.

## Не проверено

`api.osv.dev` и `api.github.com` заблокированы песочницей — полной сверки 45
залоченных пакетов с базой уязвимостей **не было**. Нужен локальный `pip-audit` и
`npm audit`.

## Побочно

В `.venv` стоят `pdfplumber`, `pdfminer.six`, `pypdfium2`, `pillow`, `cryptography` —
их нет ни в `pyproject.toml`, ни в локе, ни в одном импорте. Похоже на ручной
`pip install` под скиллы разбора брокерских PDF. При следующем `uv sync` будут снесены.


---

## Итог (2026-09-07): сделано, стек обновлён до предела

Решение владельца: обновлять максимально, кроме Python; plotly 7 взять и проверить
визуально; побочный пункт про `.venv` в репозитории не отражать.

### Версии

| | было | стало |
|---|---|---|
| pandas | 3.0.2 | 3.0.5 |
| numpy | 2.4.4 (не объявлена) | 2.5.3, объявлена |
| plotly | 6.7.0 | **7.0.0** |
| plotly.js (бандл) | 3.5.0 | **4.0.0** |
| mypy | 1.20.2 | 2.3.1 (мажор) |
| ruff | 0.15.12 | 0.16.6 |
| pytest | 9.0.3 | 9.1.1 |
| typer | 0.25.1 | 0.27.2 |
| lxml | 6.1.0 | 6.1.3 (два security-релиза) |
| esbuild | 0.24.0 | 0.28.2 |

`beautifulsoup4` удалён — ноль импортов, только упоминания в комментариях, тянул пять
пакетов. `pytest-cov` удалён — объявлен, нигде не включён. Границы сужены:
`pandas>=3.0` вместо `>=2.2` (диапазон спанил границу мажоров, где сменились
Copy-on-Write и строковый dtype), `requires-python` получил верхнюю границу, добавлен
`.python-version`. **749 тестов, mypy чист, эталон совпал — числа не сдвинулись.**

### plotly 7: взят, с новым гардом

Питоновский plotly 7 несёт `plotly.js 4.0.0`, а закоммичен был бандл 3.5.0 — расхождение
на два мажора. Реестр подтвердил требование `engines: node >=22`; владелец поставил
Node 22 и пересобрал бандл. Размер 1.23 → 1.29 МБ (+5% за два мажора), maplibre внутрь
не утёк.

**Новый тест связывает три копии версии plotly.js**: внутри собранного бандла, в
npm-пине и в том, что несёт питоновский plotly. SHA-тест этого не ловил в принципе — он
проверяет только подмену файла, а `uv lock --upgrade` двигает питоновскую сторону одну
и отправил бы в браузер спецификацию фигур, которую его рендерер не понимает.

`package-lock.json` выведен из `.gitignore` и коммитится: без него сборка бандла
невоспроизводима. `build.sh` теперь проверяет мажор Node, идёт через `npm ci` и при
рассинхроне лока говорит одной строкой, что делать, вместо полотна от npm.

### CI и окружение

`uv sync --frozen` → `--locked` в обоих workflow и в `scripts/setup.sh`. Разница
существенная: `--frozen` ставит лок как есть и **не проверяет** его соответствие
манифесту, то есть забытый перелок проезжает молча. В `setup.sh` было хуже —
`uv sync --frozen 2>/dev/null || uv sync`: ошибка подавлена, фолбэк перерезолвливал и
**переписывал `uv.lock`** в свежем клоне.

`pip install uv` без пина заменён на `astral-sh/setup-uv@v6` с явной версией и кешем.

`--no-dev` для `pages.yml` **не взят**: после `task 049` там стоит шаг `pytest`,
который гейтит деплой, а без dev-группы его нечем запускать.

### Отклонено, с замерами

- **mypy на `scripts/`** — основание отпало: два скрипта, считавшие числа для сайта в
  CI, стали командами CLI и покрыты `mypy src`. Остальное в `scripts/` — одноразовая
  оснастка, строгий режим даёт там 22 ошибки чистого churn.
- **Добор правил ruff.** Замерено по каждому: `W`, `NPY`, `TID`, `ISC`, `RET` дают ноль
  нарушений — включены бесплатно. `C4` 7, `SIM` 6, `PERF` 4, `PTH` 3, `FURB` 2, `A` 2 —
  24 механические правки ради стиля, не взяты. `DTZ` 6 — все шесть это `date.today()`
  в ингесте, то есть инжектируемые чтения часов, которые по разбору `task 048` должны
  там быть; включение потребовало бы шести `noqa`.
- **Оптимизация бандла** — задача сама замерила выигрыш в 0.05%.

### Побочно

Новый ruff форматирует код внутри markdown — пришлось исключить `*.md` и `.claude`,
иначе он переписал бы закрытые задачи в `tasks/completed/`. Правило `PLR0917` добавлено
в игнор рядом с `PLR0913`: это то же самое, посчитанное позиционно.

Не проверено: `pip-audit` и `npm audit` по базе уязвимостей — прокси закрывает npm,
а `api.osv.dev` недоступен.
