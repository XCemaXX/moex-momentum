# Raw source-of-truth (committed): tickers*, manifest.json, prices_iss/, dividends/,
# indices/, splits/. Tracked, including the decision overlays (`_acked*.json`,
# `_conflicts_resolved.json`, `_external_blacklist.json`) — but not the generated
# reports listed at the bottom.
#
# Momentum compute outputs: regenerable via `momentum compute monthly && momentum compute backtest`.
# Per task 014 policy: NOT committed.
momentum/

# Detector output — a pure function of the files above, regenerated byte-identical
# by `momentum corporate detect`. The decisions live in `_acked.json`, which stays
# tracked; committing the raw report only produced diffs nobody could read.
splits/_suspicious.json
dividends/_gaps.json
