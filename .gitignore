__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local CI mirror: `momentum site build --out _site`
_site/

# HTTP cache for all fetchers. Safe to delete:
#   .fill_cache/iss/      — ISS endpoints (tickers refresh, prices, splits, indices)
#   .fill_cache/dohod/    — dohod.ru HTML
#   .fill_cache/smartlab/ — smart-lab.ru HTML
# NOT regenerable — the only surviving copy of a dead source:
#   .fill_cache/yahoo/    — Yahoo dropped Russian prices in 2022
#   .fill_cache/tbank/    — read back by scripts/backfill/cascade_merge_dividends.py,
#                           which is wired to never re-fetch
.fill_cache/
*.tmp
